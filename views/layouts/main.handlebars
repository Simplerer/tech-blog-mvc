<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>A Tech-E-ish Blog</title>
  <link rel="stylesheet" href="/css/style.css">
</head>

<body>
  <div id="whole">
    <header>
      <h1>A Tech-E-ish Blog</h1>
      <nav>
        {{#if loggedIn}}
        <p class="links"><a href="/dashboard/{{userId}}">Dashboard</a></p>
        <p class="links"><a id="logout-link" href="/">Logout</a></p>
        {{else}}
        <p class="links"><a href="/signup">Sign Up</a></p>
        <p class="links"><a href="/login">Login</a></p>
        {{/if}}
      </nav>
    </header>
    <main>
      {{{body}}}
    </main>
  </div>
  {{#if loggedIn}}
  <script src="/js/logout.js"></script>
  {{/if}}
</body>

</html>